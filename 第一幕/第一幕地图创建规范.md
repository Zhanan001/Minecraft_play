我将根据你的第一幕剧本，制定一份详细的《地图创建技术规范》。这份规范旨在确保建筑在技术上精确实现剧本的叙事、功能和氛围要求。

### 🗺️ **第一幕地图创建技术规范**

#### **1. 核心技术与坐标规划**

* **地图种子与版本**：记录并固定地图种子，使用与服务端一致的稳定版本（如 1.20.1）。
* **坐标原点**：**建议将“中央篝火”中心点设为坐标原点 `(0, 64, 0)`**，所有其他建筑的坐标以此为基准进行规划。
* **世界预设**：建议使用“超平坦”自定义预设生成基础世界，然后通过地形插件或WorldEdit塑造河谷与森林地形，以确保对建筑区域的绝对控制。

#### **2. 分区建筑规范**

以下所有坐标均为**示例**，需根据你的实际地图尺寸调整。

**A. 中央集会广场**

* **中心坐标**：`(0, 64, 0)`
* **尺寸**：一个以原点为中心、半径30格的圆形区域。
* **篝火**：
  * 必须使用**营火**方块，并通过指令使其永久燃烧（`/data merge block 0 64 0 {Locked:true}`）。
  * 周围5格半径内，光照等级必须≥12。
* **布雷恩位置**：坐标 `(15, 64, -10)`。需在此处放置一个带有自定义名称的盔甲架，为后续NPC插件设置出生点。
* **破损棚屋**：围绕广场随机分布，但必须保证至少4个的门朝向篝火，体现场景向心力。

**B. 方尖碑及祭坛**

* **中心坐标**：`(0, 64, 80)` （位于广场正北方，与广场有明显距离感）
* **结构与材质**：
  1. **基座**（Y=64-67）：必须使用 **`crying_obsidian`（哭泣的黑曜石）** 作为核心材质，以支持紫色粒子效果。
  2. **碑体**（Y=68-88）：使用 **`purpur_block`（紫珀块）** 竖立。
  3. **能量核心**（Y=89）：放置一个**信标（Beacon）**，光束颜色设置为紫色（`/data merge block 0 89 80 {Primary:9}`）。
* **隐藏石室**：在基座内（坐标 `(0, 63, 80)`）设置一个朝下的**黏性活塞**，连接一个位于地下、包含讲台和“黯淡的符文石”的密室。活塞由**压力板**或后续任务触发的红石信号控制。
* **脉冲效果实现**：
  * 在基座四周埋设**命令方块**链，循环执行 `/particle minecraft:dust 0.5 0.0 0.8 1 ~ ~1 ~ 0.2 0.5 0.2 0 20` 产生紫色粒子。
  * 使用 `/playsound` 命令播放自定义心跳声，范围设置为全域。

**C. 灰寂森林**

* **入口坐标**：`(40, 64, 0)` （广场东侧）
* **地形与氛围**：
  1. **树木**：使用高大的云杉，但用 `/fill` 命令将部分树叶替换为 `white_wool` 或 `white_concrete_powder` 模拟灰白感。
  2. **地面**：主要使用 `podzol`（灰化土）和 `coarse_dirt`（粗土），并散落 `cobblestone`（圆石）。
  3. **薄雾**：在Y=65高度，使用区域效果云命令生成白色雾气：`/execute at @a run particle minecraft:cloud ~ ~1 ~ 0.5 0.2 0.5 0 30`，但限制在森林坐标范围内。
* **采集者营地**：坐标 `(45, 64, 5)`，放置一个**棕色地毯**和**营火**（熄灭状态）。

**D. 职业建筑区**（第8天解锁）

* **匠师铁匠铺**：`(-25, 64, 0)`。内部必须包含**铁砧、熔炉、工作台**，且熔炉内预置少量燃料和矿石。
* **元术士研究室**：`(0, 64, -25)`。核心是一个**附魔台**，下方用**玻璃柱**营造悬浮感。
* **狩魂者小屋**：`(25, 64, 0)`。门口需有**栅栏围栏**，内部有**工作台**和**熄灭的营火**。

**E. 世界事件：能量裂隙**

* **触发坐标**：森林深处 `(100, 64, 50)`。
* **战场**：清理出40x40的空地，地面替换为 `end_stone`（末地石）或 `obsidian`（黑曜石）以标记区域。
* **Boss生成点**：空地中心 `(100, 65, 50)`。此处需预留一个 **“能量畸变体”** 的刷怪点（可通过Citizens或MythicMobs插件实现）。

#### **3. 功能与机关实现规范**

* **光影控制**：使用 **WorldGuard** 插件，将“广场”和“职业区”设置为安全区（禁止怪物生成），将“森林”和“裂隙”区域的自然光照锁定为 `10`，营造昏暗感。
* **NBT数据**：所有任务相关的**箱子、告示牌、讲台**，必须在放置时使用 `/data merge block` 命令锁定其内容物或文字，防止玩家破坏。
* **粒子与音效**：
  * **全局灰烬**：在河谷上空（Y=80）运行低频粒子命令。
  * **方尖碑脉冲**：使用**记分板**或**红石时钟**控制命令方块，实现每2秒一次的高强度粒子爆发。
  * **环境音效**：使用 **Resource Pack（资源包）** 替换游戏内音效，或使用 `playsound` 命令在特定区域循环播放背景音。
* **怪物生成**：
  * **畸变蝙蝠**：仅在Y>60且光照<7的区域自然生成。
  * **徘徊的阴影**：使用插件在森林的特定坐标点（而非自然生成）刷出，并设置为中立，跟踪玩家。

#### **4. 测试与验收清单**


| 检查项         | 通过标准                                                 | 测试方法                         |
| :------------- | :------------------------------------------------------- | :------------------------------- |
| **坐标与动线** | 玩家从苏醒点能无阻碍地找到布雷恩、篝火、森林入口。       | 以生存模式新账号从头体验。       |
| **光影与氛围** | 各区域光照等级符合设定，粒子效果在视野内可见但不卡顿。   | 在白天、夜晚不同时段查看。       |
| **关键机关**   | 方尖碑石匣能按剧本在第3天弹出；符文石可共鸣。            | 模拟玩家操作流程。               |
| **怪物生成**   | 夜晚蝙蝠在营地外生成；森林阴影在雾中生成，不卡进地形。   | 挂机观察，检查插件配置。         |
| **插件兼容**   | 所有自定义方块（如带文字的告示牌）在重启服务器后仍存在。 | 重启服务器后检查。               |
| **性能**       | 在方尖碑脉冲和BOSS战时，服务器TPS保持在18以上。          | 使用`timings` 命令进行性能分析。 |

#### **5. 版本控制与协作**

1. **使用WorldEdit的 `//schematic save` 命令**，为每一个独立建筑（如“方尖碑”、“铁匠铺”）单独保存结构文件。
2. **建立文件命名规范**，例如：`V1_Act1_Central_Plaza.schem`。
3. 所有参与建筑的人员，必须在**同一版本、相同材质包**的条件下进行建设，以确保视觉效果统一。

这份规范的核心是 **“用坐标和命令固化设计”**。在开始大规模建造前，**强烈建议先用羊毛等方块在目标地点拉出每个关键建筑的外框，进行动线和空间关系的测试**，确认无误后再进行精细建造。

如果你需要针对某个具体建筑（如方尖碑的红石机关）或插件配置（如WorldGuard的区域设置）获得更详细的命令或配置示例，我可以为你进一步展开说明。
